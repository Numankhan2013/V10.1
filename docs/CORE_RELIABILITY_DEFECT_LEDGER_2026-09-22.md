# Core reliability defect ledger — 2026-09-22

Branch: `feature/marrow-core-reliability-hardening-20260922`
Base: live `feature/marrow-canonical-full-current` at `105cf02e358d8f869d745e698fc4faa32d777c92`

This ledger covers persistence, Practice/CBT lifecycle, restart, and cloud-merge defects found during the hardening pass. It does not change question content, Marrow images, explanation inventory, or release automation.

| Defect / reproduction | Root cause and affected state | Fix | Regression / status |
|---|---|---|---|
| A malformed `qbank_state_v1` reset the learner to an empty state. | `loadState()` caught every parse error and returned `defaultState()`; no schema/revision validation or recovery copy existed. Attempts, bookmarks, reviews, tests, modules, and sessions were at risk. | Added schema-v2 state validation, monotonically increasing revisions, pending journal, and `qbank_state_lkg_v2`. Boot selects the newest valid copy and displays a persistent recovery error. | `test_cross_device_sync_v1.py`: corrupt primary, valid snapshot, interrupted newer journal, unsupported revision. Local verified. |
| A quota/interrupted write could be reported only as a transient toast while callers still navigated or cleared state. | `saveState()` returned no result and destructive callers ignored failure. | All state writes route through `nkDurablePersist`; writes verify primary bytes, update the recovery copy, and roll back the journal/primary on failure. Pause, discard, Practice submit, and CBT submit retain the live state and show a persistent retry action when saving fails. | Simulated primary and recovery-snapshot failures; Pause/Practice Submit/CBT Submit failure tests. Local verified. |
| Normal Practice identity existed only in mutable `activeSession`; special modes could displace it. | Practice, CBT, Review, FSRS, Wrong/Bookmarks, and modules shared one slot. | Added versioned `normalPracticeCheckpoint` with immutable ordered membership, context, position, answers, submitted flags, timing, pending FSRS ratings, lifecycle, and per-question revision metadata. Special modes suspend rather than adopt/erase it. `activeSession` remains the compatible live surface. | Exact order/index/context and special-mode suspension tests. Local verified. |
| Legacy collapsed `questionIds` could reopen as `1 / 1`; missing IDs could silently shrink a session. | Runtime lists were trusted independently from `sessionQuestionIds`. | Resume rebuilds the full list from immutable membership. Missing IDs create a visible recoverable conflict and leave the checkpoint intact instead of filtering membership. | Existing 20-question legacy repair plus missing-membership contract. Local verified; generated browser path remains mandatory in CI. |
| Starting a second normal Practice could silently overwrite the saved one. | `startSession()` unconditionally replaced `activeSession`. | A Resume / Discard / Cancel decision is required. Discard is terminal and durable and never marks untouched questions skipped. | Unit lifecycle coverage; generated dialog/browser verification included in the existing Practice gate. |
| Practice final submission performed multiple saves and generated a new result ID on retry. | Lifecycle, skipped eligibility, result creation, and session clearing were split across wrappers. | One deterministic `practice_<sessionId>` result and terminal checkpoint are persisted before clearing/navigating. Repeated taps are idempotent. | Failed-save rollback and duplicate-submit tests. Local verified. |
| Timed CBT could be replaced and its submit path navigated after a failed save. | No exclusive-session gate; result IDs were random; `saveState()` result was ignored. | CBT now has a persisted deadline, exclusive Resume/Abandon decision, restart routing/expiry auto-submit, deterministic `exam_<sessionId>` result, deterministic attempt IDs, and failure-aware terminal persistence. | Active-CBT replacement, failed submit, successful submit, and duplicate-submit tests. Local verified. |
| Cloud sync treated all active sessions as one last-writer-wins entity. Concurrent answers could be lost and terminal state could regress. | `sessions/active` replaced `state.activeSession` wholesale. | Normal Practice syncs separately as `practiceSessions/normal`. Same-session membership must match; answers merge by per-question revisions, submitted flags union, timing takes maxima, and terminal lifecycle wins. Different sessions become an explicit conflict. Legacy normal-session envelopes migrate into the checkpoint path. | Concurrent same-session edits, membership/session conflicts, and terminal non-regression tests. Local verified. |
| A failed/in-flight sync outbox could be lost on reload if sync metadata itself tore. | Sync metadata had one best-effort direct write. | Added revisioned pending/main/LKG sync metadata writes. Existing identity-checked acknowledgements still preserve newer in-flight edits and failed entries. | Failed upload, partial batch, newer in-flight edit, repeated reconnect, and outbox recovery contracts. Local verified. |
| Backgrounding captured neither the latest question time nor a shutdown-safe outbox. | Sync only ran when the page became visible; pagehide only recovered FSRS pending ratings. | `visibilitychange(hidden)` and `pagehide` synchronously flush elapsed time/state and capture the local cloud outbox. Network completion is not required. | Static/runtime lifecycle contract; generated PWA verification required in CI. |
| Legacy Review helpers bypassed the canonical writer. | Several late scripts called `localStorage.setItem('qbank_state_v1', …)` directly. | The cross-device transform routes generated Review writes through `window.NKQBankStorage.persistExternalState`; generated-state direct-write assertions fail the sync gate. | Generated direct-write prevention assertion. CI-only until the ordered build runs. |
| Generated browser suites timed out or inspected stale markup after replacing a Practice session. | Historical content, stable-ID, presentation, hygiene, and visual suites opened unrelated questions in one browser context and assumed each call could silently replace the prior normal-Practice session. The new Resume-or-Discard contract correctly displayed a modal; same-route test helpers could then inspect the prior rendered question before the route settled. | Multi-question generated-browser helpers explicitly select **Discard and start new**, wait for the requested immutable question ID to become active, and exercise the app's same-route render path before inspecting presentation markup. This tests the replacement decision without weakening production session protection. | Reproduced in Build runs `35710024800`, `35710913892`, and `35711704217`. Run `35712597542` caught and corrected a keyword-only Playwright helper argument before exercising this assertion. Pending rerun. |

## Verification status

- `python3 tools/verify_local.py`: passed, 47 local checks. PDF generation and generated-app/APK checks are intentionally CI-only in Termux.
- Generated browser coverage now includes the major navigation smoke path, browser Back, `390×844` and `820×1180`, bookmark persistence, a fresh browser-context restart, full Practice submit, Analysis, Review Solutions, Previous/Next, grid, End Review, and Home return.
- Full Ubuntu PDF/generated PWA/browser/APK/package verification and physical-device verification remain pending until the branch is pushed and both required workflows complete.
