name: One-shot Q4 final handoff

on:
  push:
    branches:
      - automation/marrow-images-biochemistry-NKQ_BIOCHEM_COVERAGE_Q4_20260911

permissions:
  contents: write

jobs:
  finalize:
    if: ${{ !contains(github.event.head_commit.message, '[q4-final-handoff]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: automation/marrow-images-biochemistry-NKQ_BIOCHEM_COVERAGE_Q4_20260911
          fetch-depth: 0
      - name: Finalize canonical memory
        run: |
          python3 - <<'PY'
          from pathlib import Path
          state=Path('.project-memory/STATE.md')
          text=state.read_text()
          text=text.replace('## Latest image recovery batch — CURRENT_UNVERIFIED','## Latest image recovery batch — VERIFIED_HISTORY')
          text=text.replace('Classification: **CURRENT_UNVERIFIED / source-validated bounded coverage-recovery batch**. This branch owns the writer lane until exact-head canonical CI finishes or the batch is safely retired.','Classification: **VERIFIED_HISTORY / COMPLETE bounded coverage-recovery batch**. This batch no longer owns the shared writer lane.')
          text=text.replace('- Exact-head Engineering Gate and full Android/PWA/package verification are pending on this clean candidate. Production promotion remains forbidden.','- Exact certified product candidate: `25393745b3e7d859bbae1029af93986c2a8daace`.\n- Exact-head Engineering Gate `34629844685` **SUCCESS**.\n- Exact-head full Android/PWA/browser/APK/package run `34629847355` **SUCCESS**.\n- Browser source-image comparison capture, offline image bytes, packaged APK image bytes, reproducibility manifest, and PWA preview deployment passed.\n- Production promotion was skipped; `main` was not changed.')
          text=text.replace('- `NKQ_BIOCHEM_COVERAGE_Q4_20260911` is CURRENT_UNVERIFIED and owns the writer lane until exact-head CI completes or it is safely retired.','- No image batch currently owns the writer lane after the verified Q4 handoff is incorporated.')
          text=text.replace('1. Finish exact-head Engineering Gate and full Android/PWA/package verification for `NKQ_BIOCHEM_COVERAGE_Q4_20260911`.\n2. If both pass, record the exact verified candidate/run IDs, retire this batch to VERIFIED_HISTORY, and fast-forward only `feature/marrow-image-rollout-current`; never `main` or production.\n3. The following bounded recovery batch must start at `marrow__BIOCHEM_CH02_Q006:figure:1` (explanation, source pages [31], status UNTRACKED_SOURCE_VISUAL).','1. Fast-forward only `feature/marrow-image-rollout-current` to this verified handoff if its live head still equals exact base `3bc5229c573e304f1216c054457697eff6a78121`; never force, merge `main`, or promote production.\n2. Start the next bounded Biochemistry recovery batch at `marrow__BIOCHEM_CH02_Q006:figure:1` (explanation, source pages [31], status UNTRACKED_SOURCE_VISUAL).\n3. Keep Biochemistry subject status INCOMPLETE until the full coverage gate passes.')
          # Correct stale known-problem count to current post-Q4 state.
          text=text.replace('- Biochemistry still has 28 legitimate source references not learner-facing (4 tracked-unreleased + 24 untracked), plus 31 text-cue items requiring adjudication.','- Biochemistry still has 27 legitimate source references not learner-facing (4 tracked-unreleased + 23 untracked), plus 31 text-cue items requiring adjudication.')
          state.write_text(text)

          log=Path('.project-memory/SESSION_LOG.md')
          with log.open('a',encoding='utf-8') as f:
              f.write('''\n\n## 2026-09-11 — Biochemistry Q4 coverage batch fully verified\n\n- Bounded image-recovery batch `NKQ_BIOCHEM_COVERAGE_Q4_20260911` completed from exact integration base `3bc5229c573e304f1216c054457697eff6a78121`.\n- Released `marrow__BIOCHEM_CH01_Q004:figure:1` as an explanation-only PASS binding from authoritative Marrow ED8 Biochemistry page 12 / xref 25 / region `[162,433,450,649]`.\n- Production is a source-faithful 300-DPI exact-region PNG, SHA-256 `400546eaace6e3f0a2ec793f528c94722a8d2c2896f304e08cbd652277fdb03f`, 1200×901. Full-page/native-object/production inspection preserved both D-glucose and D-mannose Fischer projections and every H/OH stereochemical placement; no reconstruction or inferred detail was used.\n- Source/release validation run `34629572092` succeeded. Exact certified product candidate `25393745b3e7d859bbae1029af93986c2a8daace` passed Engineering Gate `34629844685` and full Android/PWA/browser/APK/package run `34629847355`. Browser image comparison capture, PWA offline bytes, packaged APK bytes and reproducibility checks passed. Production promotion was skipped.\n- Post-batch Biochemistry coverage: 95 raw / 94 effective / 67 released / 1 invalid metadata / 68 resolved / 4 tracked-unreleased / 23 untracked / 31 text-cue items. Subject remains INCOMPLETE.\n- Next deterministic unresolved source reference: `marrow__BIOCHEM_CH02_Q006:figure:1` (explanation, page 31, UNTRACKED_SOURCE_VISUAL).\n- No `main` merge or production promotion.\n''')
          PY
          rm .github/workflows/one-shot-biochem-q4-finalize.yml
          python3 tools/verify_project_memory.py
          git diff --check
      - name: Commit final handoff
        run: |
          git config user.name 'Numankhan2013'
          git config user.email '124483154+Numankhan2013@users.noreply.github.com'
          git add -A
          git commit -m 'Finalize verified Biochemistry Q4 image handoff [q4-final-handoff] [skip ci]'
          git push origin HEAD:automation/marrow-images-biochemistry-NKQ_BIOCHEM_COVERAGE_Q4_20260911
